 export class App {
    constructor() {
        const highlight: any = { indexWithinClass: 1 };
        const highlightGroupSeriesIndex: string = ''
        let highlightClass: string = '';

        highlightClass = `mix-hoveredHighlight_seriesIndex${highlightGroupSeriesIndex}itemIndex${highlight.indexWithinClass}`;
        highlightClass = `mix-hoveredHighlight_seriesIndex${highlightGroupSeriesIndex}itemIndex${highlight.indexWithinClass}`;
    }
}
